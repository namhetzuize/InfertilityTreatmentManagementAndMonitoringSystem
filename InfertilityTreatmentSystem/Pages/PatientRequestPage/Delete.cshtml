@page "{requestId:guid}"
@model InfertilityTreatmentSystem.Pages.PatientRequestPage.DeleteModel

@{
    ViewData["Title"] = "Delete Patient Request";
}


<div class="container mt-5">
    <div class="card border-danger shadow-lg">
        <div class="card-header bg-danger text-white d-flex align-items-center">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>
            <h4 class="mb-0">Xác nhận yêu cầu của tôi </h4>
        </div>
        <div class="card-body">
            <p>Bạn có chắc chắn muốn <strong>xóa</strong> yêu cầu  này không? Hành động này <strong>không thể hoàn tác</strong>.</p>

            <hr />


        </div>
        <div class="card-footer text-end">
            <form method="post" class="d-inline">
                <button type="submit" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Xóa
                </button>
                <a href="@Url.Page("./Details", new { requestId = Model.Request.RequestId })"
                   class="btn btn-secondary ms-2">
                    <i class="bi bi-arrow-left-circle"></i> Quay lại
                </a>
            </form>
        </div>
    </div>
</div>
