@page "{recordId:guid}"
@model InfertilityTreatmentSystem.Pages.MedicalRecordPage.DeleteModel

@{
    ViewData["Title"] = "Xóa hồ sơ bệnh án";
}

<div class="container mt-5">
    <div class="card border-danger shadow-lg">
        <div class="card-header bg-danger text-white d-flex align-items-center">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>
            <h4 class="mb-0">Xác nhận xóa hồ sơ bệnh án</h4>
        </div>
        <div class="card-body">
            <p>Bạn có chắc chắn muốn <strong>xóa</strong> hồ sơ bệnh án này không? Hành động này <strong>không thể hoàn tác</strong>.</p>

            <hr />

           
        </div>
        <div class="card-footer text-end">
            <form method="post" class="d-inline">
                <button type="submit" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Xóa
                </button>
                <a href="@Url.Page("./Index", new { recordId = Model.Record.RecordId, appointmentId = Model.Record.AppointmentId })"
                   class="btn btn-secondary ms-2">
                    <i class="bi bi-arrow-left-circle"></i> Quay lại 
                </a>
            </form>
        </div>
    </div>
</div>